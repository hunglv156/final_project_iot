# ============================================================
# REQUIREMENTS RASPBERRY PI 4 - OpenCV + TensorFlow Version
# ============================================================
# Cài đặt trên Raspberry Pi:
#   pip3 install -r requirements_rpi_opencv.txt
#
# Phiên bản này:
#   - Thay MediaPipe → OpenCV (face detection)
#   - Dùng TensorFlow/Keras (eye state classification)
# ============================================================

# TensorFlow (cho Keras model inference)
# Chọn 1 trong 2:
tensorflow>=2.13.0        # Full version (~200MB, support GPU nếu có)
# tensorflow-cpu>=2.13.0  # CPU-only (~100MB, nhẹ hơn - uncomment nếu dùng)

# Computer Vision với OpenCV (bao gồm DNN module cho face detection)
opencv-contrib-python>=4.8.0

# Numerics
numpy>=1.23.0,<2.0.0

# GPIO (thường đã cài sẵn trên Raspberry Pi OS)
RPi.GPIO>=0.7.1

# Pillow cho xử lý ảnh
Pillow>=9.5.0
