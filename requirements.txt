# ============================================================
# REQUIREMENTS RASPBERRY PI 4 - OpenCV + TensorFlow Lite Version
# ============================================================
# Cài đặt trên Raspberry Pi:
#   pip3 install -r requirements_rpi_opencv.txt
#
# Phiên bản tối ưu cho ARM64:
#   - Thay MediaPipe → OpenCV (face detection)
#   - Dùng TensorFlow Lite (eye state classification) - chỉ ~10MB
#   - Tối ưu numpy cho ARM64
# ============================================================

# TensorFlow Lite Runtime (inference-only, rất nhẹ ~10MB)
# LƯU Ý: Nếu gặp lỗi "FULLY_CONNECTED version 12" → Xem FIX_MODEL_COMPATIBILITY.md
# PHƯƠNG ÁN 1 (Khuyên dùng): Re-export model với TF 2.13
# PHƯƠNG ÁN 2: Thay tflite-runtime bằng tensorflow==2.15.0 (nặng hơn ~500MB)
tflite-runtime>=2.14.0

# Computer Vision với OpenCV (bao gồm DNN module cho face detection)
opencv-contrib-python>=4.8.0

# Numerics - tối ưu cho ARM64
numpy>=1.23.0,<2.0.0

# GPIO (thường đã cài sẵn trên Raspberry Pi OS)
RPi.GPIO>=0.7.1

# Pillow cho xử lý ảnh
Pillow>=9.5.0
